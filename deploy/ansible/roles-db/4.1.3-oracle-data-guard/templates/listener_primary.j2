# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.
#

################
# Filename......: listener.ora
# Created.......: created by SDAF
# Name..........:
# Date..........:
# @(#) $Id: //inst/inst_scripts/lmts_006_REL/tpls/ora/LISTENER.ORA#2 $
################
ADMIN_RESTRICTIONS_LISTENER = on
LISTENER =
  (ADDRESS_LIST =
        (ADDRESS =
          (PROTOCOL = IPC)
          (KEY = {{ db_sid | upper }}.WORLD)
        )
        (ADDRESS=
          (PROTOCOL = IPC)
          (KEY = {{ db_sid | upper }})
        )
        (ADDRESS =
          (PROTOCOL = TCP)
          (HOST = {{ hostname }})
          (PORT = 1521)
        )
  )
STARTUP_WAIT_TIME_LISTENER = 0
CONNECT_TIMEOUT_LISTENER = 10
TRACE_LEVEL_LISTENER = OFF
SID_LIST_LISTENER =
  (SID_LIST =
    (SID_DESC =
      (SID_NAME = {{ db_sid | upper }})
      (GLOBAL_DBNAME = {{ db_sid | upper }})
      (ORACLE_HOME = /oracle/{{ db_sid | upper }}/{{ ora_release }})
    )
 (SID_DESC =
      (SID_NAME = {{ db_sid | upper }})
      (GLOBAL_DBNAME = {{ db_sid }}_DGMGRL)
      (ORACLE_HOME = /oracle/{{ db_sid | upper }}/{{ ora_release }})
    )
  )
ADR_BASE_LISTENER = /oracle/{{ db_sid | upper }}/saptrace
